{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --production=false && (cd client && npm install && npx vite build) && echo 'Build completed'"
  },
  "deploy": {
    "startCommand": "node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}